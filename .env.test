# Throwaway config for `npm run dev -- --mode test`: points the dashboard at
# a test instance of the monitor API instead of the real one on 47113.
VITE_DATA_SOURCE=local
VITE_API_BASE=http://127.0.0.1:47999
